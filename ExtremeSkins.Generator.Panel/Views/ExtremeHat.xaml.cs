using System.Windows;
using System.Windows.Controls;

using ExtremeSkins.Generator.Resource;

namespace ExtremeSkins.Generator.Panel.Views;

/// <summary>
/// Interaction logic for ExtremeHat
/// </summary>
public sealed partial class ExtremeHat : UserControl
{
    public ExtremeHat()
    {
        InitializeComponent();
    }
    /*
     * <!-- 前レイヤーの画像 -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <exs:SkinRowPanel TitleText="{DynamicResource ExH.SelectFrontImage}"/>
        </Grid>

        <!-- 前レイヤーのフリップ画像 -->
        <Grid Grid.Row="3" Margin="0,0,0,10">
            <exs:SkinRowPanel TitleText="{DynamicResource ExH.SelectFrontFlipImage}"/>
        </Grid>

        <!-- 後ろレイヤーの画像 -->
        <Grid Grid.Row="4" Margin="0,0,0,10">
            <exs:SkinRowPanel TitleText="{DynamicResource ExH.SelectBackImage}"/>
        </Grid>

        <!-- 後ろレイヤーのフリップ画像 -->
        <Grid Grid.Row="5" Margin="0,0,0,10">
            <exs:SkinRowPanel TitleText="{DynamicResource ExH.SelectBackFlipImage}"/>
        </Grid>

        <!-- 登ってる時の後ろ画像 -->
        <Grid Grid.Row="6" Margin="0,0,0,10">
            <exs:SkinRowPanel TitleText="{DynamicResource ExH.SelectClimbImage}"/>
        </Grid>
     */
}
